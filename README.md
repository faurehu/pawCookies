### Paw Cookie Viewer

A chrome devtool made to manage the cookies in the browser.

#### Technologies:

* Redux
* React
* Webpack
* SCSS
* Chrome extension

#### Building:

* `npm install`
* `webpack`
* Go to `chrome://extensions` and load it.
